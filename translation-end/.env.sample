# Translation service (FastAPI) environment variables
# Copy to translation-end/.env and fill values as needed. Do NOT commit real secrets.

# Server
PORT=8000

# Database (optional; only if you use save-cloudinary-url or persistence)
MONGO_URL=
